#εργασια 5
#Γράψτε ένα πρόγραμμα σε Python το οποίο παίρνει ένα κείμενο από ένα αρχείο και το σπάει σε λεξεις. Αν οι λέξεις έχουν μήκος πάνω από 3 γράμματα, αφαιρέστε το πρώτο γράμμα και προσθέστε το γράμμα στο τέλος μαζί με το ay.


#ΕΙΣΟΔΟΣ ΑΡΧΕΙΟΥ
file = input('give a file as testfile.txt : ')

#ΜΕΤΑΦΟΡΑ ΛΕΞΕΩΝ ΑΠΟ ΤΟ ΑΡΧΕΙΟ ΣΕ ΜΙΑ ΛΙΣΤΑ ΜΕ ΟΝΟΜΑ "flat_list"
words = []
with open(file,'r') as f:
    for line in f:
        for word in line.split():
            words.append(word)
j=0 #μετριτης λεξεων 

for i in words:
    if (len(i) > 3):        
        temp = list(i)#δημιουργια προσωρινης λιστας για τη διαχειρηση της καθε λεξης
        flet=temp[0] #εδω κραταμε το πρωτο γραμμα
        del temp[0] # εδω βγαζουμε το πρωτο γραμμμα
        temp.append(flet)# εδω βγαζουμε το πρωτο γραμμμα στο τελος
        temp.append('ay') # εδω βαζουμε την καταληξη
        temp = "".join(temp) #εδω εννωνουμε την λιστα
        words[j] = temp # αντικαθιστουμε την παλια λεξη με την καινουρια 
    j+=1
        
print(words)
